/**
 * Solicita ao usuário dois números por meio de prompts, calcula a soma e exibe o resultado em um alerta.
 */
const number1 = Number(prompt('Enter the first number:'));
const number2 = Number(prompt('Enter the second number:'));

alert(`The sum of ${number1} and ${number2} is ${number1 + number2}.`);
